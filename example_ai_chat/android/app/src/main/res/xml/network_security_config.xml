<?xml version="1.0" encoding="utf-8"?>
<!--
  The example talks to the ai-testing proxy over plain http on the developer's
  machine. Android blocks cleartext by default, so it is re-enabled for those
  addresses only — 10.0.2.2 is how an emulator reaches the host. Everything
  else still requires https.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
